# 2026-09-08

## 新建 docs/business-flows.md（业务动作流程设计）

- 15 个核心动作（注册/登录/个人预约创建取消/活动创建/审核/报名/取消报名/创建者与管理员取消活动/活动结束/球场管理/发帖/评论/点赞）按统一格式（触发者/前置条件/核心流程/事务边界/并发控制/状态变化/失败情况/结果）落成文档。
- 并发方案约定：只用现有表行做悲观锁（user 行锁保护 ≤2 上限；court 行升序锁保护时段冲突；activity 行锁保护满员计数），固定加锁顺序 user → court 升序 → activity，不新增表。
- 附录 A 详析三个并发场景（同场抢时段 / 抢最后一个名额 / 建活动锁场与个人预约并发）。
- 附录 B 列出 6 条「需求未明示的默认值」（报名窗口=开始前、创建者取消须未开始、区间不跨天、最早约明天等），实现前可调整。

## docs/api.md 第一版 API 设计（覆盖占位内容）

- 覆盖：认证与用户 / 场馆与球场 / 个人预约 / 活动 / 报名 / 社区 / 管理员 / 统一响应 / 错误码 / 分页 / 边界共 12 章。
- 约定：前缀 /api、JSON snake_case、时间 yyyy-MM-dd'T'HH:mm:ss；分页 page/page_size，列表 data={list,total,page,page_size}。
- 统一响应推荐 {code,message,data}，HTTP 只表达传输/认证/授权/格式（200 承担全部业务结果），均标注「待最终技术决策确认」。
- 关键接口语义与 business-flows 对齐：创建预约/活动/报名/取消的并发失败结果（COURT_ALREADY_RESERVED、ACTIVITY_FULL 等）在文档中显式声明。
- 管理端统一 /api/admin/*，含球场管理、活动审核队列、活动取消、删帖，以及规则 11 的管理员特殊取消个人预约。
- 仍待决策：认证机制（Session vs JWT，建议 ADR-0007）、统一响应与 HTTP 策略是否按推荐方案落定。

## 建表脚本生成（sql/）

- `sql/schema.sql`：建库 badminton_community + 9 张表 DDL。含全部唯一约束与索引、CHECK（status 值域 / end>start / max_members>=1 / reservation 归属恰一非空 (user_id IS NULL) <> (activity_id IS NULL)）；comment.post_id、post_like.post_id 外键 ON DELETE CASCADE。
- `sql/data.sql`：管理员 admin123/123456（BCrypt `$2b$10$i/rV...7d0HK`，注释标注上线前必须改密）、1 家场馆、6 块球场（1-6 号，venue_id=1，依赖全新库自增）。
- `sql/README.md`：更新为执行说明（schema → data；MySQL 8.0.16+）。

## 本机建库与测试（MySQL 8.0.41）

- 本机 MySQL：`mysql.exe` 在 `C:\Program Files\MySQL\MySQL Server 8.0\bin\`；root 密码 `123456`（Git Bash 里 PATH 中的 `mysql` 解析异常，须用完整路径 `.exe`）。
- 已建库 `badminton_community` + 9 张表；data.sql 重灌成功。
- 踩坑：Windows 下 mysql 客户端执行含中文的 data.sql 必须加 `--default-character-set=utf8mb4`，否则中文报 1406 Data too long。
- 约束验证全部通过：重复 username / 同馆球场重名 / 重复报名 / 重复点赞均 1062；role 值域、activity 时间与人数、reservation 归属恰一非空（user/activity 皆空或皆有值都报 3819）；删帖级联删评论与点赞；同场重叠预约 DB 允许（符合设计——冲突属 Service 层）。
- BCrypt 哈希已验证与明文 123456 匹配。
- 执行测试注意：需 `--force` 才能跑完整组「预期失败」用例。

## docs/decisions.md 追加 ADR-0007/0008/0009

- ADR-0007：认证 JWT（无状态），认证与授权分离，MVP 无黑名单/无双 Token；登出=客户端丢弃 Token。
- ADR-0008：原生 MyBatis（Mapper+XML/注解），复杂/并发 SQL 显式控制，不采用 MyBatis-Plus 自动 CRUD。
- ADR-0009：统一响应 Result&lt;T&gt;（code=0 成功，业务错误码遵循 api.md §10），HTTP 策略遵循 api.md，Controller 不手工拼装。
- 冲突核对：与 api.md/database.md/business-flows.md 无冲突；api.md 中「认证待 ADR / 响应待确认」占位由 ADR-0007/0009 落定（可选后续把 api.md 标注改为已确认，未改）。

## Spring Boot 脚手架 + auth 垂直切片（编译/启动/冒烟通过）

- 决策（用户选定）：仓库根目录即工程；自研 JWT 拦截器 + jjwt；按业务模块分包（module/user + common/config/security）；使用 Lombok；JDK17 + Maven(mvn.cmd 3.9.16) + Spring Boot 3.3.5 + 原生 MyBatis starter 3.0.3 + mysql-connector + spring-security-crypto(BCrypt) + jjwt 0.12.5。
- 结构：包名 com.badmintoncommunity；src/main/java/{BadmintonCommunityApplication, common(Result/ResultCode/BusinessException/GlobalExceptionHandler), config(WebConfig), security(JwtProperties/JwtUtil/LoginUser/UserContext/AuthInterceptor), module/user(entity/mapper/dto/controller/service)}；application.yml 连本机 MySQL（root/123456）。
- 踩坑记录（重要）：
  1) 用户全局 MAVEN_HOME=D:\develop\apache-maven-3.9.4-bin\apache-maven-3.9.4 损坏（缺 boot/plexus-classworlds），且 Git Bash 下 mvn/mvnw shell 脚本对中文/路径处理异常；解决办法：下载干净 Maven 到 D:\develop\apache-maven-3.9.16，用 PowerShell 执行 mvn.cmd（先 Remove-Item Env:MAVEN_HOME）编译。不建议再动用户原目录，可提议用户自行修复 MAVEN_HOME 指向新目录。
  2) WorkBuddy Bash/PowerShell 沙箱会注入 SERVER__PORT=0 环境变量 → 覆盖 yml 的 server.port=8080 导致随机端口；显式传 --server.port=8080 可覆盖（yml 已加注释）。
  3) spring-boot:run 的 fork 进程加载类路径失败（NoClassDefFoundError SpringApplication），改用 mvn package + java -jar。
- 冒烟测试 10 项全部符合 api.md：错误密码→INVALID_CREDENTIALS(200)；注册成功；重名→USERNAME_TAKEN(200)；登录返回 token；无 token→401；假 token→401；坏 JSON→400 PARAM_INVALID；/users/me GET/PUT 正常；登出 200。
- 运行方式：mvn.cmd -DskipTests package 后 java -jar target/backend-0.0.1-SNAPSHOT.jar --server.port=8080。
- 待办：继续 venue/activity/reservation/community 模块（每个都按 mapper+service+controller+dto+错误码对齐 api.md）；修复/提示用户全局 Maven；端口问题是否保留 --server.port 覆盖写法。

## venue 场馆模块完成（冒烟通过）

- 新增 module/venue（Venue/Court entity+mapper、VenueVO/CourtVO/CourtCreateRequest/CourtUpdateRequest、VenueService、VenueController、AdminCourtController）+ module/reservation 的 ReservationMapper 与 OccupancySlot（占用查询、countFutureEffective）。
- JSON 全局改 snake_case（application.yml jackson.property-naming-strategy=SNAKE_CASE，对齐 api.md §1.1；注意响应字段如 venue_id 已生效）。
- WebConfig 白名单新增 /api/venues、/api/venues/**、/api/courts/*/occupancy（公开只读）；管理端 /api/admin/courts* 需认证且 Service 校验 role==1（requireAdmin→FORBIDDEN）。
- 动态 SQL 注解注意：MyBatis 注解里 <script> 必须包整条 SQL。
- 冒烟：场馆/球场/占用(匿名200)；新增球场成功(id=8)；重名→COURT_NAME_DUPLICATED(200)；普通用户→FORBIDDEN(403)；改名/停用/恢复成功；无 token→401。测试球场已清理（court 剩 6）。
- Windows 打包踩坑：jar 运行中（java -jar）会锁文件导致 repackage 无法 rename，需先停进程再 package。
- 用户偏好补充：明确表示"不想读代码/注释再多也像天书"，只想要每个 Service 的 3 句话业务说明 + 进度汇报。后续每个模块交付时先给"这模块做什么业务的 3 句话"，再写代码，代码里注释照旧但不要求他读。
